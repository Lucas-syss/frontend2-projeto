const AnnouncementBar = () => {
    return (
        <div className="w-full bg-primary text-primary-foreground py-2.5 text-center text-xs tracking-widest uppercase font-mono">
            Free Worldwide Shipping on orders over $150
        </div>
    );
};

export default AnnouncementBar;
